/**
 * NotificationBell wrapper — re-exported from client components.
 * Usable in Sidebar (src/components/layout/Sidebar.tsx) via @/ alias.
 */
export { NotificationBell, default } from '@/client/components/NotificationBell';
